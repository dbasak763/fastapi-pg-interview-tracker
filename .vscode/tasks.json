{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Wait for FastAPI",
      "type": "shell",
      "command": "for i in {1..75}; do curl -fsS http://127.0.0.1:8000/ >/dev/null && exit 0; sleep 0.2; done; echo 'FastAPI did not become ready within 15 seconds.' >&2; exit 1",
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated",
        "clear": true
      }
    }
  ]
}
